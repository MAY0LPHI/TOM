services:
  - type: web
    name: nazuna-bot
    env: node
    buildCommand: apt-get update && apt-get install -y ffmpeg && npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.0.0
